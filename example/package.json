{
	"private": true,
	"name": "example",
	"version": "0.0.0",
	"scripts": {
		"clean:dist": "rimraf dist/*",
		"clean:build": "rimraf build/*",
		"node:build": "parcel build node.js --no-cache",
		"web:build": "yarn clean:dist && parcel build index.html --no-cache",
		"web:start": "yarn clean:dist && parcel index.html --no-cache"
	},
	"devDependencies": {
		"@parcel/transformer-inline-string": "^2.0.0",
		"parcel": "^2.9.1",
		"parcel-packager-wasm": "*",
		"parcel-transformer-assemblyscript-codument": "0.1.0-alpha",
		"rimraf": "^5.0.0"
	}
}
